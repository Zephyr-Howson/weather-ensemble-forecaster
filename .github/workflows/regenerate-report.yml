name: Regenerate accuracy report

# Split out from daily-collect.yml so a failure/race here can never also cost
# that job's actual data collection, and so the report can be refreshed on
# demand (workflow_dispatch) without redoing the whole 30-location collection
# - previously the only way to do that was a manual local run.
#
# Three triggers, because a single one can't cover every case that should
# refresh the report:
# - `workflow_run` covers the normal nightly case: once daily-collect.yml
#   finishes collecting fresh data.
# - `push` on data/weather.db or data/weather_periods.db covers a manual
#   local push (a backfill, a dedupe, a retrain, a reconcile) that changes
#   the underlying data outside the nightly run - this is exactly the step
#   that's been forgotten/done by hand repeatedly so far.
# - `workflow_dispatch` covers "just regenerate it right now" with no
#   underlying data change at all (e.g. after a report-rendering code fix).
on:
  workflow_run:
    workflows: ["Daily forecast collection"]
    types: [completed]
  push:
    branches: [main]
    paths:
      - data/weather.db
      - data/weather_periods.db
  workflow_dispatch:

concurrency:
  group: regenerate-report
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  regenerate:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          # A real incident: this used to pin to
          # `github.event.workflow_run.head_sha`, on the assumption that it
          # reflects the triggering run's own final commit. It doesn't -
          # empirically, a run regenerated a report that was missing data
          # that already existed in daily-collect's own just-pushed commit,
          # meaning this checkout out something older/stale instead. Checking
          # out `main` by name sidesteps the ambiguity entirely: it always
          # resolves to whatever is actually on the branch right now.
          ref: main

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: python -m pip install -U pip
      - run: pip install -e .

      - name: Regenerate accuracy report across all locations
        run: |
          weather-ensemble \
            --all-locations \
            --accuracy-report reports/accuracy_report.html

      - name: Commit and push the regenerated report
        # Same non-rebasing retry pattern as daily-collect.yml's DB commit -
        # see that step's comment for why. reports/accuracy_report.html is
        # regenerated wholesale from the DB every time, but
        # data/weather_report_archive.db (see report_archive.py) accumulates
        # real, irreplaceable history across runs - a rolling 30 days of
        # gzip-compressed report snapshots - exactly like weather.db/
        # weather_periods.db do, so it needs the same re-sync-then-reapply
        # treatment: a race against another push can only cost this run's
        # own update (one more fetch-and-retry), never silently discard
        # snapshots another run already committed.
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp reports/accuracy_report.html /tmp/accuracy_report.html
          cp data/weather_report_archive.db /tmp/weather_report_archive.db

          for attempt in 1 2 3 4 5; do
            git fetch origin main
            git reset --hard origin/main
            cp /tmp/accuracy_report.html reports/accuracy_report.html
            cp /tmp/weather_report_archive.db data/weather_report_archive.db
            git add reports/accuracy_report.html data/weather_report_archive.db
            if git diff --cached --quiet; then
              echo "No changes to commit"
              exit 0
            fi
            git commit -m "Regenerate accuracy report $(date -u +%Y-%m-%dT%H:%M)"
            if git push origin HEAD:main; then
              echo "Pushed on attempt $attempt"
              exit 0
            fi
            echo "Push rejected (attempt $attempt/5) - retrying against latest main"
            sleep $((attempt * 5))
          done
          echo "::error::Failed to push accuracy_report.html after 5 attempts"
          exit 1

      # GitHub Pages deployment lives in its own workflow (deploy-pages.yml),
      # triggered by this workflow completing.
